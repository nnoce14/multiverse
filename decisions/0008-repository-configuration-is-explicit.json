{
  "decision_id": "decision-0008",
  "title": "Repository configuration is explicit in 1.0",
  "status": "accepted",
  "iteration": 1,
  "context": "The tool needs a way for repositories to declare what they want managed and how. An explicit model is safer during the period when the core business abstractions are still maturing.",
  "decision": "In 1.0, the tool uses one unified repository configuration model. Every managed resource and every managed endpoint must be declared explicitly. Every declared object must explicitly select a provider. Provider-specific configuration is allowed, but it extends rather than replaces required core business fields. Defaults are intentionally minimal in 1.0.",
  "alternatives": [
    "Infer managed objects from application code or directory structure.",
    "Use convention-over-configuration with many built-in defaults.",
    "Allow separate configuration files per provider type."
  ],
  "rationale": "An explicit unified model keeps repository behavior understandable, deterministic, and suitable for behavior-first design and TDD. It also avoids premature inference and hidden conventions while the core business abstractions are still maturing.",
  "consequences": [
    "Repositories must declare their managed objects directly rather than relying on discovery or inference.",
    "The tool validates configuration as a business declaration surface before any implementation-specific behavior is considered.",
    "Future versions may streamline configuration once the explicit model has been proven stable."
  ],
  "reversal_trigger": "Once the core business model is stable and proven in practice, conventional defaults and inference may be introduced in a post-1.0 release with a governing decision record.",
  "source_ids": ["src-multiverse-old-reference"],
  "linked_rubric_dimensions": [
    "business_truth_alignment",
    "architecture_boundary_integrity",
    "refusal_safety"
  ]
}
