# Frontend Runtime Verification Lead Memory

- Browser-facing slices need an explicit runtime check when UI or client bundles change.
- A clean static build does not prove the browser can execute the served bundle.
